import ReactDOM from "react-dom/client";
import SortingVisualizer from "../components";

function App() {

  return(
    <SortingVisualizer></SortingVisualizer>

  )
  
}

ReactDOM.createRoot(document.getElementById("root")).render(<App />);


